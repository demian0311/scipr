package scipr

import scala.xml.Elem

class ConfigParser {
    
    def parse(xml: Elem): Option[Map[String, Server]] = {
            None
            // go thru the xml and try
            // to instantiate the servers
            // that we recognize
    
    }
}